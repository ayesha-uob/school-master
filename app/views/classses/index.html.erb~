<p id="notice"><%= notice %></p>

<h1>Listing Classses</h1>

<table>
  <thead>
    <tr>
      <th>Class name</th>
      <th>Room no</th>
      <th>Section</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @classses.each do |classs| %>
      <tr>
        <td><%= classs.class_name %></td>
        <td><%= classs.room_no %></td>
        <td><%= classs.section %></td>
        <td><%= link_to 'Show', classs %></td>
        <td><%= link_to 'Edit', edit_classs_path(classs) %></td>
        <td><%= link_to 'Destroy', classs, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Classs', new_classs_path %>
